# zond-rs/homebrew-tap

Homebrew formulae for [Zond](https://github.com/zond-rs/zond).

```sh
brew install zond-rs/tap/zond
```

The formula installs the prebuilt macOS binary from each release, for Apple
Silicon and Intel.
